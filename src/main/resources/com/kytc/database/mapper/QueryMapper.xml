<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kytc.database.dao.QueryDao">
	<select id="list" resultType="java.util.Map">
	  	${sql}
	</select>
	<select id="count" resultType="Long">
	  	${sql}
	</select>
	<select id="listOne" resultType="java.util.LinkedHashMap" parameterType="String">
		${sql}
	</select>
	<select id="listReport" resultType="com.kytc.database.dto.ReportDTO" parameterType="String">
		${sql}
	</select>
</mapper>